{
  "name": "@sima-land/ui-quarks",
  "version": "0.0.0",
  "description": "Sima-land design tokens as a React components/helpers",
  "repository": "ssh://git@gitlab.sima-land.ru:7888/dev-dep/dev/packages/ui-quarks.git",
  "scripts": {
    "build:before": "rm -rf temp dist",
    "build:main": "ts-node --project tsconfig.node.json .build/prebuild.ts && tsc",
    "build:after": "rm -rf temp",
    "build": "npm run build:before && npm run build:main && npm run build:after",
    "dist": "npm run build && cp package.json dist/package.json && cp README.md dist/README.md"
  },
  "author": "www.sima-land.ru team",
  "license": "Apache-2.0",
  "devDependencies": {
    "@sima-land/linters": "^2.2.0",
    "@svgr/core": "^6.5.1",
    "@svgr/plugin-jsx": "^6.5.1",
    "@svgr/plugin-prettier": "^6.5.1",
    "@types/babel__core": "^7.20.0",
    "@types/fs-extra": "^11.0.1",
    "@types/lodash": "^4.14.191",
    "@types/react": "^18.0.28",
    "fast-glob": "^3.2.12",
    "fs-extra": "^11.1.0",
    "lodash": "^4.17.21",
    "prettier": "^2.8.4",
    "svgo": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "peerDependencies": {
    "react": "^17.0.2"
  }
}
